[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn backend.server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/periods"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
